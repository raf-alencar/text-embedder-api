{
  "name": "text-embedder-api",
  "version": "2.0.0",
  "description": "Self-hosted HTTP API for vector embeddings via OpenRouter with async PostgreSQL polling",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "node-cron": "^3.0.3"
  }
}
